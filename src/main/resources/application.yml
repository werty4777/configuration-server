spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/werty4777/config-repo
          default-label: main
server:
  port: 8888


eureka:
  client:
    healthcheck:
      enabled: true
    serviceUrl:
      defaultZone: https://eurekaclient.herokuapp.com/eureka
